# Proxy API calls to the Render backend so the client can keep using /api/* paths
/api/*  https://vitalcare-0srl.onrender.com/api/:splat  200

# SPA fallback — every other path serves index.html so React Router works on refresh
/*      /index.html                                     200
